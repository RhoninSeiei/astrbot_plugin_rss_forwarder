# 更新日志

## v0.7.1

- RSS 与 Twitter/Nitter 来源新增来源级 TLS 证书校验设置，可按单个来源配置 `verify_ssl`。
- 新增来源诊断 Plugin Page 与 `/rss probe <feed_id>` 命令，用于比较默认网络、来源代理、严格证书校验和宽松证书校验的访问结果。
- `verify_ssl` 对旧配置和新来源均默认启用；该设置只影响 RSS 文档、Nitter 时间线与推文详情请求，正文、图片、视频及其他媒体下载继续采用严格证书校验。
- RSS 抓取失败日志新增任务 ID、脱敏来源地址、代理状态、HTTP 客户端和经过清理的异常摘要，便于识别故障来源并避免记录鉴权信息。

## v0.7.0

- 轮询任务正式支持 5 段 cron 表达式，不再把 `jobs[].cron` 回退为 interval 轮询。
- 新增 `jobs[].aggregate_enabled` 聚合推送模式，可在任务触发后把本批次新条目交给 LLM 生成动态标题和分段摘要。
- 新增聚合图卡渲染，`jobs[].aggregate_render_mode=image` 时使用本地 Pillow 生成卡片，并尽量把条目配图渲染到对应段落中。

## v0.6.7

- 文本模式下恢复“正文 + 图片”同一条消息发送；远程 RSS 图片会在发送前缓存为本地 JPEG，减少源站反盗链、格式兼容和 QQ 远程转存失败导致的富媒体错误。
- RSS 源的 `proxy_url` 会传递到原文图片缓存请求，用于需要代理访问的源站图片。

## v0.6.6

- 普通 RSS 文本推送改为先发送正文并确认去重，再将原文图片和视频作为附加消息发送，避免 QQ 富媒体上传失败导致同一条新闻反复重试。
- `feeds[].send_images` 和 `feeds[].send_videos` 现在会影响普通 RSS 源的媒体组件发送；关闭后不会再附带该 RSS 源的图片或视频。
- `feeds[].max_new_items` 现在也可限制普通 RSS 源每轮解析数量；`jobs[].batch_size` 开始限制单轮成功推送条数。

## v0.6.5

- 修复普通 RSS 条目仅包含 `image_url` 时，简洁模式附带图片未随标题发送的问题。
- 日报 `render_mode=image` 改为使用本地 Pillow 生成图片，使用预制 RSS 标识、插件 logo、签名区和本地字体资源，减少对 AstrBot 官方 T2I 服务的依赖。

## v0.6.4

- 轮询任务新增 `jobs[].compact_mode_send_images`，可在简洁模式下继续附带条目图片。
- 文本推送中，简洁模式开启图片后仍只显示标题，不显示来源、时间、摘要和网页链接。
- 图片图卡模式中，简洁模式开启图片后会先发送标题图卡，再发送原文图片。

## v0.6.3

- 推送目标新增 `targets[].compact_mode`，支持 `inherit`、`compact`、`normal` 三种模式，可在同一个轮询任务内让不同目标使用不同展示形式。
- 多目标轮询任务的内容确认改为按 `job + target + item` 记录，单个目标发送失败时，后续轮询只补发未确认目标，已发送目标不会重复收到同一条内容。
- 同一轮询任务继续只执行一次语义重复判定，目标级展示策略不会造成额外语义判定模型调用。

## v0.6.2

- 日报任务新增 `daily_digests[].semantic_merge_enabled`，可在生成日报前使用模型把多个来源报道的同一事件合并为一条。
- 日报语义合并支持独立选择 `semantic_merge_provider_id`、候选数量上限和置信度阈值，失败或超时时会保留原日报条目。
- 轮询任务新增 `jobs[].compact_mode_enabled` 简洁模式，开启后该任务只推送标题，不显示来源、时间、摘要、网页链接和原文媒体。

## v0.6.1

- README 新增脱敏面板截图和插件图标，配置说明使用占位源和占位会话标识展示 RSS、Twitter、目标和轮询任务的配置方式。
- RSS 源新增 `proxy_url` 面板字段，抓取层会按源配置使用 HTTP 代理，并改用更通用的浏览器式请求头提升站点兼容性。
- 图片渲染首次失败时会刷新 AstrBot 官方 t2i 端点列表并重试一次，减少官方端点列表变化后仍命中故障端点的问题。
- 日报任务新增 `daily_digests[].llm_timeout_seconds`，可为聚合日报单独设置更长 LLM 等待时间，填 `0` 时继承全局配置。

## v0.6.0

- 新增任务级语义重复判定，可在单个 `jobs[]` 中独立开启，适合多个 RSS 源报道同一新闻事件时减少重复推送。
- 新增 `jobs[].semantic_dedup_provider_id`，可在 AstrBot 模型列表中为当前轮询任务选择语义判定模型。
- 新增语义候选保留时间、候选数量上限和置信度阈值配置，过期候选会从判定输入中移除以控制模型消耗。
- 语义判定失败、超时或返回无效结果时会继续推送原条目，避免模型异常阻断 RSS 推送。
- README 新增配置图示和从零开始配置说明，使用占位源和占位会话标识展示 RSS、Twitter、目标和轮询任务的配置方式。

## v0.5.2

- 修复日报 `render_mode=image` 在 AstrBot 主动推送场景下调用渲染接口失败的问题，改为使用插件自身的 `Star.html_render` 能力。
- 修复图片渲染结果未包装为图片消息链的问题，日报图卡和普通 RSS 图卡在主动推送时都会按图片消息发送。
- 新增日报图片渲染失败后的文本回退，避免渲染接口异常导致日报完全无法发送。

## v0.5.1

- 运行时注册名统一为 `astrbot_plugin_rss_forwarder`，并自动从旧 `astrbot_rss` 状态目录迁移去重记录。
- 插件面板的源配置改为 `RSS/Twitter 源配置`，新增条目时可分别选择 `RSS/Atom 源` 与 `Twitter/Nitter 源`。
- 旧配置中的 `__template_key=feed` 会按 `source_type` 自动迁移为 `rss_feed` 或 `twitter_feed`，迁移后会保存回面板配置。
- 新增 `feeds[].max_new_items`，Twitter/Nitter 源默认每轮只抓取最新 1 条新推文，填 `0` 时抓取全部新推文。
- README 补充 Twitter/Nitter 功能参考来源与差异说明，便于用户按实际需求选择插件。

## v0.5.0

- 新增 Twitter/Nitter 源支持，可在 `feeds[]` 中将 `source_type` 设置为 `twitter`，按推主用户名采集时间线并复用现有 job/target 推送。
- Twitter 推文会转换为统一条目结构，继续使用现有任务级去重 TTL、发送前重复拦截、翻译链路和日报归档。
- 新增 `feeds[].send_images` 与 `feeds[].send_videos`，可分别控制 Twitter 推送是否附带图片和视频。
- 新增 `display_source`、`display_time`、`display_link`，文本推送与图片图卡均可控制是否显示来源、时间、链接。
- 新增 `feeds[].send_link`，可单独关闭 Twitter 推送里的原推文链接。
- 新增 `feeds[].nitter_url` 与 `feeds[].proxy_url`，支持为 Twitter 源配置 Nitter 镜像站和抓取代理。
- Twitter 图片和视频会优先缓存到插件数据目录后以本地文件发送，减少代理环境下媒体发送失败。
- 修复 Nitter 详情页临时失败或限流时 `since_id` 过早推进的问题，失败推文会在后续轮询继续尝试。
- 暂未迁移链接识别与合并转发能力，实时推送采用“文字 + 图片 + 视频”的普通消息形式。

## v0.4.3

- 修复任务级去重时效上线后，旧 7 天缓存记录过期仍会被当作新内容推送的问题。
- 去重判断现在会按当前任务的 `dedup_ttl_seconds` 重新计算旧记录有效期，并在命中时刷新 `expire_at`。
- 长保留窗口 RSS 源可以继续使用 50 天任务级去重，避免旧文章在源站仍可见时重新推送。

## v0.4.2

- 新增 `jobs[].dedup_ttl_seconds`，支持按轮询任务覆盖全局去重记录保留时间；填 `0` 时继续继承全局值。
- 为长期保留旧条目的 RSS 源补齐任务级去重时效配置能力，便于按 feed 实际保留窗口分别设置。
- 新增 `docs/llm/` 本地 Markdown 文档库，记录项目结构、线上运维约束、关键决策与路线图，减少后续开发与运维时的上下文丢失。

## v0.4.1

- 调整插件面板中的草稿保存行为：新建 `feed`、`target`、`job` 默认关闭，可先保存未填完的条目。
- 配置校验仅针对启用中的 `feed`、`target`、`job` 与 `daily_digest` 执行，避免半填配置导致插件重载失败。
- `targets[].unified_msg_origin` 的面板提示改为“启用时必须填写”，减少填写过程中的误操作。

## v0.4.0

- 新增 `daily_digests[]` 日报任务，可按每日固定时间汇总指定 RSS 源并发送摘要。
- 日报任务与即时推送分离，未绑定即时推送任务的 feed 也可单独参与日报归档与发送。
- 新增 `/rss digest run [digest_id]`，便于立即验证某个日报任务。
- 日报支持 `text` 与 `image` 两种独立渲染模式，并支持在 GUI 中修改默认提示词模板。
- 新增日报素材归档、日报状态记录、日报专用抓取任务与定时发送任务。

## v0.3.6

- 版本号同步更新，用于插件市场识别后续更新。

## v0.3.5

- 增强重复推送抑制：发送前查重优先使用原文身份信息，并将图片 `sha256` 纳入发送指纹。
- 调度器启动时清理残留 `rss-job-*` 任务，减少热重载后旧任务继续运行造成的重复推送。

## v0.3.4

- 新增第二层内容去重，基于规范化后的 `link` 生成指纹键。
- 调度器同时检查主去重键与 `link` 指纹键，降低源站 `guid` 变化导致的重复发送。

## v0.3.2

- 翻译顺序调整为 `LLM -> Google -> GitHub Models`，其中 GitHub Models 作为第二后备。
- AstrBot 插件面板中的翻译配置说明已与实际顺序保持一致。
- 中文、英文、日文 README 补充了三种翻译服务的配置获取方式。

## v0.3.1

- 翻译链路新增 GitHub Models 回退层，支持从 `data/github.token` 或环境变量读取 GitHub token。
- 翻译诊断命令新增 GitHub Models 状态输出，便于确认 token、模型与代理配置。

## v0.3.0

- 翻译输出改为中文标题与中文摘要。
- 推送翻译链路改为严格串行回退，并增加翻译路径日志。
- 新增 `/rss test` 诊断命令，并增强 RSS 图片提取与附图能力。
